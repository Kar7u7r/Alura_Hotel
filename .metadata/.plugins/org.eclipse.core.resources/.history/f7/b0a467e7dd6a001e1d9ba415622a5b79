package DAOS;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;

import model.category;
import views.ReservasView;

public class ReservasBusquedaDAO {
	private Connection con;

	public ReservasBusquedaDAO(Connection con) {
		this.con = con;
	}

	public List<category> list() {
		List<category> result = new ArrayList<>();
		try {
			final PreparedStatement statement = con.prepareStatement("select id" + ",cname from category");
			try (statement) {
				final ResultSet resultSet = statement.executeQuery();

				try (resultSet) {
					while (resultSet.next()) {
						var category = new model.category(resultSet.getInt("id"), resultSet.getString("cname"));

						result.add(category);
					}
				}
			}
		} catch (SQLException e) {
			throw new RuntimeException(e);
		}

		return result;
	}

}
